[package]
name = "chemu8_core"
version = "0.1.0"
edition = "2024"

[features]
default = []
wasm = ["dep:getrandom"]

[dependencies]
rand = "0.10.1"
getrandom = { version = "0.4", features = ["wasm_js"], optional = true }
